import {Text, View} from 'react-native';
import React from 'react';
import { MyButton, MyInput, SearchInput } from '../../components/common';

const HomeScreen = () => {
  return (
    <View>
   <SearchInput/>
 
   
    </View>
  );
};

export default HomeScreen;
